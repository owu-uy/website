---
const { count, title, subtitle } = Astro.props;
---

<div class="text-center text-white">
  <p class="text-4xl font-bold text-yellow-400">{count}</p>
  <p class="text-2xl font-semibold">{title}</p>
  <p class="text-base">{subtitle}</p>
</div>
